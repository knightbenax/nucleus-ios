# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Nucleus' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'UberRides', '~> 0.7'
  pod 'Alamofire', '~> 4.5'
  pod 'RZTransitions'
  pod 'ImagePicker'

  # Pods for Nucleus

  target 'NucleusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NucleusUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
